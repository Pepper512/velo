# Plaintext IMAP on loopback only — throwaway test server, never exposed.
ssl = no
disable_plaintext_auth = no
mail_location = maildir:/srv/mail/%u

# Default Dovecot advertises UIDPLUS; nothing to do.
