1. Verdict: **CHANGES REQUESTED**.

2. Findings:

**[M] F-01** — `src/components/email/ThreadView.tsx` — Fix (a) only covers the *initial* `null` sentinel. `ownAddresses` is still `activeAccount && aliases ? [activeAccount.email, ...aliases.map(...)] : null` with no check that `aliases` belongs to the current account, and `setAliases(null)` lives in the effect (after paint). On `activeAccountId` A→B the first render has **B’s** `activeAccount` and **A’s** `aliases`, so `ownAddresses` is a mixed list, `instantIntro` can be non-null, and `handleInstantIntro` can fire (`if (!lastMessage || !instantIntro) return` then `aliases ?? []`). That is the same class of bug (a) claimed to close (“the intro never runs on a half-known address list”). Reset in render (e.g. `{ accountId, aliases } | null` and treat a mismatch as `null`), not only in the effect.

**[M] F-02** — `src/services/composer/instantIntro.ts` / `ThreadView.tsx` — Fix (b) splits reasons in `instantIntroUnavailableReason` (`No sender…` / `The last message is your own` / `Nobody to introduce you to`), and the JSDoc says “the key path checks for `null`”. The key path does not: `handleInstantIntro` still gates only on `!instantIntro` and `isNoReplyAddress(...)`. The own-from string is computed **before** `buildInstantIntro` (`if (own.has(bareAddress(target))) return "The last message is your own"`; else `buildInstantIntro(...) ? null : "Nobody…"`). This delta does not show `buildInstantIntro` rejecting own-from, so `introUnavailableReason` can be a string while `instantIntro` is a payload. Toolbar disable (outside this delta) and the shortcut then disagree. Gate the handler on `introUnavailableReason == null` (or the same helper), not only on `instantIntro`.

**[L] F-03** — `src/components/email/ThreadView.tsx` — Fix (d) is only in `replyTarget` (`const replyTo = message.reply_to?.trim(); const from = message.from_address?.trim(); return replyTo || from || null`). The no-reply guard is unchanged: `isNoReplyAddress(lastMessage.reply_to ?? lastMessage.from_address)`. `??` does not treat `""` / `"   "` as absent, so a blank `reply_to` plus a no-reply `from_address` is not filtered. Use the same `replyTarget` (or the same trim/`||` rule) here.

**[N] F-04** — `src/stores/composerStore.ts` / composer UI (not in delta) — Fix (c) is one atomic `set`: `fromEmail: opts?.fromEmail ?? null` instead of `fromEmail: null` plus a later `setFromEmail`. That removes the two-write window. Whether a composer effect still overwrites `fromEmail` from the intro’s To/Cc after open **cannot be verified from this delta**.

**[N] F-05** — toolbar / `introUnavailableReason` consumer (not in delta) — ThreadView now passes `introUnavailableReason` which is `null` when available, a specific string when the helper fails, or `"Instant Intro is not ready yet"` when `!lastMessage || !ownAddresses`. Confirm the child disables on any non-null string and enables only on `null`; that wiring is outside the delta.

3. What you verified and found correct:

- **(a) initial load:** `aliases: SendAsAlias[] | null` starting at `null` means `ownAddresses` is `null` until the fetch, so first paint is no longer `[primary]` with aliases silently omitted. Empty `[]` after load stays truthy and correctly becomes `[activeAccount.email]`.
- **(b) reason strings:** helper tests cover available → `null`, missing sender, own last message with others on the thread, and empty introduction; loading is a fourth string, not the old catch-all.
- **(c) store API:** `openComposer({ fromEmail })` is a single options object; `composerOptionsForIntro` is a pure payload (mode, to/cc/bcc, subject, opener+quote, thread ids, `fromEmail`); the store test shows a resolved From is kept and a later `openComposer({ mode: "new" })` resets `fromEmail` to `null` (Reply / Reply All / Forward / draft restore that omit `fromEmail` still get `opts?.fromEmail ?? null`, i.e. the previous `null`).
- **(d) blank `reply_to`:** `replyTarget` uses trim + `||`; tests for `"   "` and `""` fall through to `from_address` on both `replyAllRecipients` and `buildInstantIntro`.
- **(e) first name:** Unicode strip `/^[^\p{L}\p{N}]+|[^\p{L}\p{N}]+$/gu` plus `?.` on `[0]` (strict index); tests for quotes/comma, `Élodie`, punctuation-only fallback; opener HTML-escape test still hits `<`, `>`, `&` without relying on edge-stripped markup.
- **`composerOptionsForIntro`:** header match uses the non-primary alias; no header match falls back to primary (via `resolveFromAddress`, not reimplemented here); no aliases → `fromEmail: null`, matching the old `if (from) setFromEmail` skip.
