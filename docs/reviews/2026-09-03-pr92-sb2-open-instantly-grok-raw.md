The review needs the full brief and commit diff from the offloaded prompt. I’ll read that file first so the SB-2 findings are based on the complete text, not the truncated excerpt.1. Verdict: **CHANGES REQUESTED.**

2. Findings:

[H] F1 — `src/components/email/ThreadView.tsx` — REQ-2.1 is not met: cached messages are applied in `useEffect`, so they are not on the first paint, and a warm cache can still show the skeleton. The design called for `useState(() => peek(...) ?? [])` with `loading = !peeked`. The commit instead peeks only after paint (`const cached = threadMessageCache.peek(...)` then `setMessages(cached); setLoading(false)`), and the `useState` initializers are not in this hunk. Context you gave (and *What exists* 5) is that `ThreadView` stays mounted and only `thread` changes, so a lazy `useState` would not have been enough on `j`/`k` either — the cache has to be read **during render**. Consequences: (a) first mount still uses today’s initial `loading === true` → one paint of three `MessageSkeleton`s even when `peek` would hit; (b) on `j`/`k`, the first paint is still the **previous** thread’s `messages` (loading is already false) and only the next paint is the cached thread. The `cancelled` flag and `finally` correctly drop a result after a switch, and `setMessages((prev) => (cached && sameMessages(prev, fresh) ? prev : fresh))` is the right React 19 bailout *after* that later paint — it does not fix the first one. Uncached `setLoading(true)` matches today, as required.

[M] F2 — `src/services/threads/messageCache.ts` — REQ-2.2’s shipped module is not in the textual diff (`Binary files /dev/null and b/src/services/threads/messageCache.ts differ`), so LRU-30, the `(accountId, threadId)` key, and **clear on `velo-sync-done`** cannot be verified against source. The factory tests (`createMessageCache`, `{ capacity: 2 }`, `cache.clear()`) never assert default capacity 30, never fire `velo-sync-done`, and never import the `threadMessageCache` singleton that `ThreadView` / `EmailList` use. That is the whole REQ-2.2 surface. Related race, necessarily inferred rather than read: if `load` writes on resolve with no generation token, an in-flight `load`/`prefetch` that started before the clear can re-insert pre-sync rows after the listener runs. I cannot confirm whether the binary file closes that.

[M] F3 — `src/components/layout/EmailList.tsx` — the warmup effect’s deps are `[activeAccountId, selectedThreadId, visibleThreads]`. Timer/job cleanup is correct for a *selection* change (`let job` is the effect binding; `clearTimeout` plus `job?.cancel()` after the 150 ms quiet period). `visibleThreads` is the problem: the brief already says every `velo-sync-done` reloads the page and **replaces the array**. That identity change re-runs this effect, cancels an in-flight warm-up, and waits another 150 ms. That is acceptable *once per reload* if the cache was just cleared. It is not acceptable if `visibleThreads` is a new array on unrelated `EmailList` re-renders — the quiet timer never fires and `job` is still `null` in cleanup, so REQ-2.3 warming starves. The preceding (unchanged) memo ends with `[filteredThreads, activeLabel, activeCategory, categoryMap, bundledCategorySet, heldThreadIds]`; **if** that memo *is* `visibleThreads`, identity is stable except on those inputs and this does not thrash on every render. That binding is **not in the added lines** — uncertainty, and there is no EmailList test with fake timers to prove cancel-vs-restart.

[L] F4 — `src/services/threads/messageCache.test.ts` / `ThreadView.tsx` — tests never mount `ThreadView`, so they do not catch F1, the `cancelled` drop, or `loading` transitions. `sameMessages` is only ids+dates in order (`expect(sameMessages([msg("a", 1)], [msg("a", 2)])).toBe(false)` etc.), which matches the brief’s fingerprint, but a same-id/same-date body edit (local draft / `moved_to` tombstone that does **not** dispatch `velo-sync-done`, per your context) keeps `prev` and does not replace. `prefetch` cancel is tested only as “don’t start the next id; keep the in-flight row” — there is no test that a newer selection from `EmailList` actually calls `job.cancel()`. Default capacity 30 and the window listener are untested (see F2).

[L] F5 — `src/components/email/ThreadView.tsx` — `getMessagesForThread` remains imported in the same file (`import { getMessagesForThread, type DbMessage } from "@/services/db/messages"` plus the new cache import). The replaced effect was the load site cited in *What exists* 5. If nothing else in the file still calls it (rest of file not in this diff — uncertainty), that is an unused import on the SB-2 commit.

[N] F6 — `src/services/threads/messageCache.test.ts` — cancel does not abort the current SQLite `SELECT`; the test documents keeping the paid-for `t1` row. That matches “cancels a pending warm-up” if pending means “not yet started / not the next id”. Fine, but it is why F2’s post-clear write-back matters.

3. What I verified and found correct:

- **REQ-2.3 neighbour set:** `prefetchOrder` is next three then previous one, clipped at the ends, empty when the selection is missing (`neighbours.ts` / `neighbours.test.ts`). `PREFETCH_DELAY_MS = 150`.
- **REQ-2.3 quiet period + cancel plumbing in `EmailList`:** `setTimeout(..., PREFETCH_DELAY_MS)` then `prefetch`; cleanup `clearTimeout` + `job?.cancel()`; a change before 150 ms never starts a job (`job` still `null`). Sequential skip-cached / fail-and-continue / cancel-before-next are specified by the factory tests.
- **SWR *after* the first paint:** `load` always runs; `cancelled` drops `then` and `finally`; `setMessages` updater keeps `prev` when the peek-hit fingerprint matches (React 19 `Object.is` bailout); uncached path still `setLoading(true)` then `setLoading(false)`.
- **REQ-2.2 as tested on the factory (not the singleton):** peek miss → load stores; key includes account; `load` re-queries and replaces; LRU via peek recency with injected `capacity: 2`; `clear()` empties; failed loader leaves no entry.
- **REQ-4 for this commit:** store, DB query modules, `ThreadCard`, and the keyboard map are not in the diff; only `ThreadView` + `EmailList` wiring plus the new cache/neighbours units.
