1. Verdict: CHANGES REQUESTED.

2. Findings:

[M] F-01 — `src/constants/helpContent.ts` — In `src/constants/helpContent.ts:1121-1123`, the help entry text states `"Reduce effects (Settings > General) turns off the animated background..."` and is attached to a category with `relatedSettingsTab: "general"`. However, as established in the brief (*What exists* 6, REQ-1.1, and Design), the toggle is located in Settings → Appearance (`src/components/settings/SettingsPage.tsx:548-555`, outside the diff). A user following this help entry will navigate to Settings > General and will not find the toggle.

[M] F-02 — `src/styles/globals.css` — In `src/styles/globals.css:125-136`, `.reduce-motion .glass-panel`, `.glass-modal`, `.glass-backdrop`, and `[class*="backdrop-blur"]` are stripped of `backdrop-filter`, but `.reduce-motion .backdrop-animate` (lines 133-136) only sets `animation: none; background-color: rgba(0, 0, 0, 0.2);`. REQ-1.2 mandates removing every `backdrop-filter` (explicitly calling out `backdropIn` and overlay blurs), and the brief's Design explicitly specifies: `"under .reduce-motion, backdrop-filter: none (and -webkit-) for the three glass classes and for .backdrop-animate"`. If `.backdrop-animate` carries a static `backdrop-filter` in its base rule (lines 398-413 outside the diff), omitting `backdrop-filter: none` leaves the blur active.

[M] F-03 — `src/styles/globals.css` — In `src/styles/globals.css:151-154`, `.reduce-motion .hover-lift:hover:not(:active)` sets `box-shadow: none;` alongside `transform: none;`. REQ-1.2 explicitly mandates: `"disable hover-lift transitions and transform, press-scale, and stagger-in. Layout SHALL not change (same paddings, borders, shadows stay)"`. At specificity (0, 4, 0), `box-shadow: none;` strips any underlying resting box-shadow (such as those carried by `.glass-panel` or elevated cards outside the diff) whenever the mouse hovers over the element, causing shadows to blink off rather than stay resting.

[M] F-04 — `src/App.tsx` — In `src/App.tsx:296-298`, platform detection is gated strictly on `savedReduceMotion === null ? await readPlatform() : "unknown"`. If `savedReduceMotion` is any unrecognised or corrupted non-null string (e.g. `"maybe"`, which is explicitly asserted to default on for Linux in `src/services/effects/reduceEffects.test.ts:28-30`), `readPlatform()` is bypassed, `platform` evaluates to `"unknown"`, and `resolveReduceEffects` returns `value: false`. The gate should test `savedReduceMotion !== "true" && savedReduceMotion !== "false"`.

[L] F-05 — `src/components/settings/SettingsPage.tsx` — In `src/components/settings/SettingsPage.tsx:551-552`, the description is rendered as `"Flat panels, no blur or animation — quieter on the CPU (on by default on Linux; also fixes flickering on some GPUs)"`. REQ-1.1 strictly specifies the copy: `"Flat panels, no blur or animation — quieter on the CPU (recommended on Linux)"`.

[L] F-06 — `src/App.tsx` — In `src/App.tsx:300`, the store update is executed conditionally via `if (reduce.value) ui.restoreReduceMotion(true);`. If `uiStore` was initialized or modified to `true` (e.g. in test suites or re-initialization passes), resolving a stored `"false"` will not reset the in-memory store to `false`. Updating unconditionally via `ui.restoreReduceMotion(reduce.value)` ensures idempotent synchronization.

[L] F-07 — `src/services/effects/reduceEffects.test.ts` — In `src/services/effects/reduceEffects.test.ts:33-41`, every test case for `readPlatform` supplies a custom mock loader argument (`await readPlatform(async () => ...)`). The default parameter (`() => import("@tauri-apps/plugin-os")`) is never exercised. Additionally, there are no unit or integration tests verifying `App.tsx` boot-time restoration or `SettingsPage.tsx` toggle rendering.

3. What you verified and found correct:

- **REQ-1.3 Session Default & Persistence**: `resolveReduceEffects` cleanly separates `value` and `persisted`. On Linux with no stored setting, it resolves `value: true, persisted: false`. In `uiStore.ts:230`, `restoreReduceMotion` updates Zustand state without triggering `setSetting`, preserving the unpersisted session default until the user explicitly clicks the toggle (`setReduceMotion`).
- **Pop-out Window Safety**: `readPlatform` in `src/services/effects/reduceEffects.ts:36-44` safely catches errors when importing or invoking `@tauri-apps/plugin-os`, gracefully returning `"unknown"` and defaulting off without throwing an unhandled rejection in pop-out windows lacking the `os` capability.
- **CSS Specificity for Tailwind Blurs**: In `src/styles/globals.css:125-131`, `.reduce-motion [class*="backdrop-blur"]` has specificity (0, 2, 0) as an unlayered rule, ensuring it reliably neutralises `-webkit-backdrop-filter` and `backdrop-filter` set by Tailwind utilities.
- **Motion Neutralisation**: Blobs are hidden via `display: none` (`globals.css:121-123`), `.stagger-in` animation is neutralized (`animation: none`), `.hover-lift` transition is disabled (`transition: none`), and `.press-scale:active` transform is reset (`transform: none`) without altering box model dimensions or padding.
