The review brief is truncated in the chat, so I’ll read the offloaded prompt for the complete diff before judging REQ-1 through REQ-4.1. Verdict: **CHANGES REQUESTED.**

2. Findings

**[M] II-1** — `src/components/email/ThreadView.tsx` (alias state + `ownAddresses`) — REQ-1.3 / REQ-1.1 require own addresses to include send-as aliases, but `ownAddresses` is not complete for a window after mount (and after account change if this instance stays mounted). Evidence: `const [aliases, setAliases] = useState<SendAsAlias[]>([]);` then a `useEffect` on `activeAccountId` that fetches `getAliasesForAccount` and only then `setAliases(...)`; `ownAddresses` is `activeAccount ? [activeAccount.email, ...aliases.map((a) => a.email)] : []`. Until that promise resolves, `buildInstantIntro` runs with only the primary email. A last message To’d to an alias plus a third party will keep the alias in `to` (user emails themselves) and a last message To’d only to that alias can look like “someone to introduce,” so the button enables and `b` opens a composer. The effect also never `setAliases([])` when `activeAccountId` changes, so if `ThreadView` does not remount on account switch (not in this diff), the previous account’s aliases sit in `ownAddresses` beside the new primary until the new fetch wins. `handleInstantIntro` does not wait on a loaded flag. Cheap fix: treat intro as unavailable until the fetch for the current account settles, and clear aliases when the account id changes.

**[M] II-2** — `ThreadView.tsx` `handleInstantIntro` (openComposer then `setFromEmail`) — From-alias selection is a separate store write after `openComposer`, uncoordinated with the composer’s own From effect (that effect is not in this diff; do not treat the next sentence as proven). Evidence: `openComposer({ mode: "replyAll", to: instantIntro.to, ... })` then `const from = resolveFromAddress(aliases, lastMessage.to_addresses, lastMessage.cc_addresses); if (from) useComposerStore.getState().setFromEmail(from.email);` with the comment that the composer picks From from the reply’s To/Cc and intro has removed the user from both. That workaround is necessary in concept, and it is the right headers to read, but: (a) it uses the same possibly-empty `aliases` as II-1, so `resolveFromAddress` can return nothing and the `if (from)` skip leaves whatever From `openComposer` defaulted; (b) if Composer’s effect runs after paint and assigns From from the draft’s To/Cc (or resets From on open), it will overwrite this write — To/Cc no longer contain the account, so the effect would not find an alias. Same-tick Zustand updates can make this work if the effect is `if (match) setFromEmail(match)` and does not fall back. That is unverified here. There is no test of this path. Prefer one atomic open (pass From into `openComposer` if that API exists) or set From in the same store update the composer effect cannot clobber.

**[L] II-3** — `ThreadView.tsx` ActionBar wiring — REQ-1.3 only requires “a reason,” but every non-`noReply` miss uses one string. Evidence: `introUnavailableReason={instantIntro ? null : "Nobody to introduce you to"}`. That is also used when there is no `lastMessage`, no `activeAccount`, or the last message is mine (`buildInstantIntro` returns null for own introducer). The done-when line asked for that copy on “to me alone”; own-message is explained in Help, not on the button. `noReply` still wins the title via ActionBar (`title={noReply ? "This sender does not accept replies" : ...}`), which is correct.

**[L] II-4** — tests — Instant Intro’s React path is only covered at ActionBar (click/disabled/title) and the dispatcher (`b` → `velo-instant-intro`). There is no test that `handleInstantIntro` no-ops on `noReply` / null intro, concatenates `openerHtml + buildQuote`, passes `threadId` / `inReplyToMessageId`, or calls `setFromEmail`. II-2 can ship silently.

**[L] II-5** — `instantIntro.ts` `replyTarget` — Introducer is `message.reply_to ?? message.from_address` then `target.trim()`. An empty-string `reply_to` does not fall through to `from_address` (`??` only skips null/undefined), so `buildInstantIntro` returns null even when `from_address` is a real introducer. The brief says this is the same expression Reply All uses; if `reply_to` is always null or a non-empty string in `DbMessage`, this is dead. Not proven from this diff.

**[N] II-6** — `instantIntro.ts` `buildInstantIntro` — REQ-1.3’s `noReply` case is not in the pure module; ThreadView’s handler returns on `isNoReplyAddress(lastMessage.reply_to ?? lastMessage.from_address)` and ActionBar disables with `noReply || introUnavailableReason !== null`. Fine for this PR as long as those two stay in lockstep with the `noReply` prop (computation of `noReply` is not in the diff).

**[N] II-7** — `ActionBar.tsx` title `"Instant Intro (b)"` — Matches REQ-3.2 and the existing Forward `(f)` pattern in the same block. A rebound `action.instantIntro` will not update the tooltip. Spec asked for that string; not a miss.

3. What you verified and found correct

- REQ-1.1 / 1.2 recipient rule: introducer is `reply_to ?? from_address`, Bcc is that chip as written (trimmed); To/Cc are reply-all chips minus own minus introducer; order kept; duplicates collapsed by `bareAddress` (lower-case, display-name chips); introducer repeated in To/Cc is stripped and not duplicated in Bcc. Tests cover the Alice/Bob outcome, `reply_to` chip in Bcc, alias/case chips, To+Cc Bob dedupe, Cc-only third party, null headers.
- REQ-1.3 unavailability in the pure function: no introducer, introducer ∈ own (including alias chip in tests), To and Cc empty after the subtract. UI: key no-ops when `!instantIntro` or `noReply`; button disabled with the no-reply title or the intro reason; ActionBar test asserts three disabled no-reply controls and that a disabled intro click does not fire.
- REQ-2.1: first word of `from_name`, else local part of the introducer (including a display-name chip); `escapeHtml` on the name only; test `"<b>Alice</b> Smith"` → `Thanks &lt;b&gt;Alice&lt;/b&gt;...`. Opener shape is exactly `<p>Thanks {name}, moving you to Bcc.</p>`.
- Subject: `Re: {subject}` without double `Re:`; null subject `Re: `.
- Composer open payload: `mode: "replyAll"`, `bcc`, `openerHtml + buildQuote(lastMessage)`, `threadId: lastMessage.thread_id`, `inReplyToMessageId: lastMessage.id` — matches the brief’s Reply All fields. `buildQuote` itself is unchanged in this diff.
- Keyboard: `action.instantIntro` default `b` in Actions; dispatcher emits `velo-instant-intro` only with a selected thread; tests for both; ThreadView subscribes with cleanup on `handleInstantIntro` (no leak from that effect).
- REQ-3.2 / 3.3: Handshake after Forward, same disabled class pattern on that button; Help entry + tip; `CLAUDE.md` table row.
- REQ-4 as far as this diff goes: `handleReply` / `handleReplyAll` / `handleForward` not edited; inline reply and context menus not touched; `bareAddress` moved byte-for-byte into `emailUtils.ts` and imported from `autoReminders.ts`; no new dependency.
- Listener hygiene: `cancelled` flag on the alias fetch; `addEventListener` / `removeEventListener` paired.
- Comma-in-display-name split inherited on purpose (brief *Not doing*).
