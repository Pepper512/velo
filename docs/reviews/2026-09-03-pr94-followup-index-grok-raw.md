I'll read the full review prompt first so the verdict is based on the complete plan and diff, not the truncated excerpt.## 1. Verdict

**APPROVE**

The shipped SQL matches the approved plan, is total on the shapes that matter, and the N−1 (post-#88) app can run against the expanded schema. Findings below are test-gap / residual, not blockers.

## 2. Findings

**[L] F1 — `src/services/db/migrations.test.ts`** — The demotion test never uses two accounts, a `created_at` tie, or `status IS NULL`. The unique key is `(account_id, thread_id)` and `thread_id` is only unique *with* `account_id` (composite FK on `threads`, outside this diff, migration 6). A plausible regression that dropped `account_id` from the index — `ON follow_up_reminders(thread_id) WHERE status = 'pending'` — would still pass both new tests, which only ever insert `acct-1` / `acct-2` with distinct `t1`/`t2`/`t9`. Ties are the reason `rowid DESC` exists; nothing asserts it. NULL status is the named residual of the plan; nothing asserts it stays outside the index. The SQL is right; the tests would not catch those three edits.

**[L] F2 — `src/services/db/migrations.test.ts`** — After demotion, test 1 only does `expect(index).toBeDefined()` on `sqlite_master.name`. That would accept a non-unique leftover with the same name. Totality of *this* path (seed duplicates → UPDATE → CREATE UNIQUE INDEX → second INSERT refused) is never checked in one sequence: the UNIQUE throw lives in a separate case that starts from an empty table and never runs the UPDATE. Mitigated because both cases execute the same migration 29 SQL via production `runMigrations()` (splitter + pinned transaction: **outside this diff**), and the cancelled-row inserts in test 2 prove the constraint is not a full unique on `(account_id, thread_id)`. Still a seam.

**[L] F3 — `src/services/db/threads.splitTabs.test.ts`** — The re-frame matches approved option 1 and still tests a real, reachable shape (one pending beside cancelled/triggered history). It no longer tests what Gemini H1 tested. `COUNT(DISTINCT t.id)` / `GROUP BY` are unexercised once only one row matches `status = 'pending'`. The comment says the forbidden state is covered by `"refuses a second pending row for a thread"` — that test covers the **schema**, not reader de-dupe. Worse, the unchanged assertions are not in the hunk, so they almost certainly still only check “one thread.” With pending `remind_at = 500` and cancelled `100`, a reader that `MIN(remind_at)`s across all statuses would still report one thread and this test would stay green. Pinning the listed reminder at `500` would give the re-framed test teeth without reseeding a forbidden row. Not a blocker: Jim approved dropping the impossible-state regression.

**[N] F4 — `src/services/db/migrations.ts`** — Demotion correctness if the `NOT IN` subquery were re-evaluated live after some rows are already `'cancelled'`: a fully empty winner set makes `id NOT IN ()` true for every remaining pending row and would cancel the keepers, after which `CREATE UNIQUE INDEX` succeeds and the user has zero pending reminders. The extra `SELECT id FROM (…)` layer is the usual SQLite materialization workaround; they used `ROW_NUMBER` not `RANK`, so ties cannot leave two `rn = 1` rows. Test 1 proves the intended outcome on better-sqlite3. A `WITH … AS MATERIALIZED` (3.35+) would make the snapshot explicit. Production engine is the Tauri v2 bundled SQLite, **not in this diff**; Tauri v2 is well above 3.25 (`ROW_NUMBER`) and 3.35. Measured in the plan on better-sqlite3 3.53.4, not on the plugin.

**[N] F5 — `src/services/db/migrations.ts` (comment) / rollback story** — The keep rule is newest `created_at`, not soonest `remind_at`. If a real duplicate ever existed, the user could lose the earlier reminder time. Disclosed in the plan; expected to touch zero rows. Pre-#88 `ON CONFLICT(account_id, thread_id)` remains invalid against a *partial* index (plan measurement, **outside this diff**). N−1 is post-#88 select-then-update-or-insert; it cannot write a second pending row inside one webview. Two webviews racing (mutex is per-webview, **outside this diff**) now get `UNIQUE constraint failed` instead of a duplicate; the plan says `ActionBar` / `autoReminders` already catch and log.

**[N] F6 — `src/services/db/migrations.ts`** — `CREATE UNIQUE INDEX IF NOT EXISTS` is redundant if the runner really wraps both statements plus the ledger insert in one transaction that rolls back on any non-“duplicate column” error (**outside this diff**, stated in the brief). Harmless, and the right belt given App init swallowing non-credential failures so a stuck 29 would silently freeze every later migration.

No [H] or [M]. Nothing here should fail totality, split, or N−1.

## 3. Verified-correct

- **Demotion target matches the index.** Both sides use `status = 'pending'` only. `NULL` status is in neither the UPDATE nor the partial index; multiple NULLs for one thread cannot make `CREATE UNIQUE INDEX` throw. Empty string / `'Pending'` similarly stay outside. Agrees with every reader filtering `= 'pending'` (**readers outside this diff**).
- **One keeper per `(account_id, thread_id)`.** `ROW_NUMBER() … ORDER BY created_at DESC, rowid DESC` cannot assign `rn = 1` to two rows (`RANK`/`DENSE_RANK` could, and then the index create would throw forever). `id` is `TEXT PRIMARY KEY` so `NOT IN` has no NULL-trap. Table is not `WITHOUT ROWID` (v6 DDL, **outside this diff**), so `rowid` exists and is unique even when `created_at` ties or is NULL (NULL sorts last under SQLite `DESC`; a timestamped row wins).
- **Shapes the UPDATE ignores correctly.** Cancelled/triggered history is excluded by `WHERE status = 'pending'` on both the outer UPDATE and the window input — test 1’s `r-hist` stays `'cancelled'`. A second thread’s single pending (`r-other` on `t2`) stays pending. Multiple accounts are separate partitions (SQL; not tested, F1). Orphans whose thread/account was deleted are still in `follow_up_reminders`; the SQL never joins `threads`/`accounts`, so FKs/CASCADE (**outside this diff**) do not affect totality.
- **`CREATE UNIQUE INDEX` after a correct demotion.** Indexed columns are `NOT NULL`. Partial `WHERE status = 'pending'` then sees at most one row per key. Remaining throw paths are: demotion never ran; a trigger re-created a pending row mid-statement (v6 DDL has none, **outside this diff**); or the name already exists as a *different* index and `IF NOT EXISTS` skips — first introduction of this name, and test 1 `DROP INDEX IF EXISTS` before re-apply.
- **Statement split.** The `sql` string contains exactly two `;`, both at `BEGIN…END` depth 0. No `BEGIN`/`END` tokens, no `;` inside strings or comments (comments are in the JS block, not the SQL). Window/`OVER (` uses parentheses, which the stated splitter does not treat as depth. Trailing `;` after the `CREATE INDEX` matches house style for 27/28 (**outside this diff**). Empty fragment after the last `;` is the same as every prior migration.
- **Idempotence / partial failure.** One pinned transaction for all statements plus the ledger insert (**outside this diff**): any throw rolls back UPDATE, index, and ledger, and 29 retries next launch. Re-run on already-unique pending rows: UPDATE is a no-op; `IF NOT EXISTS` makes CREATE a no-op if a non-transactional leftover index existed. `runMigrations` skips versions already in `_migrations`, so 29 cannot fire on a DB that already applied it, and cannot run before v6 creates the table if migrations run in array order (**outside this diff**; 29 is appended after the v28 `smtp_password` entry).
- **Will not fire on the wrong DB.** Fresh install: 1–29 in order, empty `follow_up_reminders`, UPDATE is a no-op, index created (test 3 checks ledger 29 and zero rows; ledger 29 implies both statements committed **given** the transactional runner). Upgrade: only missing versions. Downgrade to N−1: 29 stays in the ledger and is ignored by an array that ends at 28; index remains; post-#88 writer does not violate it.
- **N−1 / N+1.** Expanding-only: no `DROP` of `idx_followup_thread`, no `CHECK`/`NOT NULL` on `status`, no query changes, no `ON CONFLICT` reintroduced. N−1 select-then-update-or-insert either updates the existing pending row (same index keys) or inserts the first pending. Resurrecting a reminder while a cancelled row exists is insert-a-new-pending, which the partial index allows. Pre-#88 is already broken on this table and stays broken. Contract `DROP INDEX idx_followup_pending_unique` is named in the comment and not run.
- **Tests that do prove their claim.** Test 1: forbidden seed (two pending + history + other thread) → older `'cancelled'`, newest `'pending'`, history untouched, other thread untouched, index name present; would fail if UPDATE cancelled everyone or if CREATE INDEX did not run after the `DROP`. Test 2: second pending with a **different** `id` (`p1` vs `p2`) so the throw cannot be the PRIMARY KEY; two cancelled + one triggered allowed; `COUNT = 4`. Test 3: expected empty DB records 29. `seedBefore29` follows the v24 ledger-delete + drop-object pattern (**outside this diff**).
- **Non-goals held.** Nothing deleted; `'cancelled'` is existing vocabulary; no new writer; pairing-gate contract step is comment-only.
